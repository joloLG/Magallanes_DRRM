<?xml version="1.0" encoding="utf-8"?>
<!-- Splash screen layout with both images -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/splashBackgroundWhite">

    <!-- Images Container - Side by Side -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <!-- MDR Image - 200dp x 200dp -->
        <ImageView
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@drawable/splash_mdr"
            android:contentDescription="MDR Logo"
            android:scaleType="fitCenter" />

        <!-- Spacing between images -->
        <View
            android:layout_width="20dp"
            android:layout_height="match_parent" />

        <!-- Bulan Logo - 200dp x 200dp -->
        <ImageView
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@drawable/splash_logo"
            android:contentDescription="Bulan Logo"
            android:scaleType="fitCenter" />

    </LinearLayout>

    <!-- Spacing -->
    <View
        android:layout_width="match_parent"
        android:layout_height="32dp" />

    <!-- App Name Text -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="M.E.A."
        android:textColor="#FF8C00"
        android:textSize="28sp"
        android:textStyle="bold"
        android:fontFamily="sans-serif-medium"
        android:letterSpacing="0.1" />

    <!-- Tagline -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Magallanes Emergency App"
        android:textColor="#666666"
        android:textSize="14sp"
        android:textStyle="normal" />

</LinearLayout>
